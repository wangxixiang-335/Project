# 项目运行指南

## 📋 前置要求

1. **Node.js** 版本 >= 16.0.0
2. **npm** 版本 >= 7.0.0
3. **数据库**: Supabase（已配置）

## 🚀 运行步骤

### 1. 启动后端服务器

#### 方法一：使用推荐脚本（推荐）
```bash
cd d:/Work/Project
node start_server.js
```

#### 方法二：使用npm脚本
```bash
cd d:/Work/Project
npm run start
```

#### 方法三：开发模式（自动重启）
```bash
cd d:/Work/Project
npm run dev
```

**后端启动成功后显示：**
```
🚀 启动后端服务器...
✅ 服务器进程已启动
服务器运行在端口 8090
环境: development
```

### 2. 启动前端应用

#### 进入前端目录
```bash
cd d:/Work/Project/temp-frontend
```

#### 安装依赖（如果尚未安装）
```bash
npm install
```

#### 启动前端开发服务器
```bash
npm run dev
```

**前端启动成功后显示：**
```
  VITE v4.5.0  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

## 🌐 访问地址

- **前端应用**: http://localhost:5173
- **后端API**: http://localhost:8090/api

## 📱 登录测试

### 教师账号
- **邮箱**: teacher1763449748933@example.com
- **密码**: 任意密码（开发环境）

### 学生账号  
- **邮箱**: student1763606837482@example.com
- **密码**: 任意密码（开发环境）

## 🔍 功能验证

### 1. 教师功能验证
登录后可以访问：
- ✅ **成果管理**: 显示教师个人成果（当前为空）
- ✅ **成果查看**: 显示所有学生成果（2个项目）
- ✅ **数据看板**: 显示统计图表
- ✅ **发布页面**: 发布新成果功能

### 2. 学生功能验证
登录后可以访问：
- ✅ **项目展示**: 查看所有已通过的项目
- ✅ **个人中心**: 管理个人信息
- ✅ **项目提交**: 提交新项目

## 🛠️ 开发命令

### 常用npm命令
```bash
# 启动开发服务器（自动重启）
npm run dev

# 启动生产服务器
npm run start

# 运行测试
npm test

# 代码检查
npm run lint

# 代码格式化
npm run format
```

### 测试API
```bash
# 测试所有API端点
node test_all_apis.js

# 测试数据库连接
node simple_db_test.js

# 验证修复后的功能
node final_verification_test.js
```

## 🔧 故障排除

### 1. 端口占用
如果8090端口被占用：
```bash
# 查看端口占用
netstat -ano | findstr :8090

# 结束占用进程
taskkill /PID <进程ID> /F
```

### 2. 前端5173端口被占用
```bash
# 查看端口占用
netstat -ano | findstr :5173

# 或使用其他端口
npm run dev -- --port 3000
```

### 3. 数据库连接问题
检查环境变量配置：
```bash
# 查看当前配置
cat .env

# 测试数据库连接
node test_connection.js
```

### 4. 依赖安装失败
```bash
# 清除缓存
npm cache clean --force

# 删除node_modules重装
rm -rf node_modules
npm install
```

## 📁 项目结构

```
d:/Work/Project/
├── src/                    # 后端源码
│   ├── routes/             # API路由
│   ├── middleware/         # 中间件
│   ├── config/            # 配置文件
│   └── utils/             # 工具函数
├── temp-frontend/         # 前端源码
│   ├── src/              # React组件
│   └── public/           # 静态资源
├── supabase/            # Supabase配置
└── scripts/             # 测试脚本
```

## 📊 当前数据状态

### 数据库统计
- **教师用户**: 1个
- **学生用户**: 1个  
- **成果总数**: 2个
  - 待审核: 1个
  - 已打回: 1个
  - 已通过: 0个

### 已修复的问题
- ✅ API 400错误修复完成
- ✅ API 500错误修复完成
- ✅ React key prop警告修复
- ✅ 数据库字段匹配修复

## 📞 技术支持

如果遇到问题：
1. 查看控制台错误信息
2. 检查服务器日志
3. 运行测试脚本验证
4. 参考故障排除部分

---

**快速启动命令总结：**
```bash
# 启动后端
cd d:/Work/Project && node start_server.js

# 启动前端（新终端）
cd d:/Work/Project/temp-frontend && npm run dev
```

祝您使用愉快！🎉